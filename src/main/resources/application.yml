server:
  port: 8080

spring:
  application:
    name: erp-muhasebe

  datasource:
    url: jdbc:mysql://localhost:3306/erp_muhasebe?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 12345678asd
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # update = var olan yap?y? koruyarak tablo olu?turur
    show-sql: true       # SQL sorgular?n? console'da göster
    properties:
      hibernate:
        format_sql: true # SQL'i okunabilir biçimde yaz
